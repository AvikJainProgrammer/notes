questions:
  - question_id: 8
    question_type: write_code
    language: python
    question_text: |
      `Pygame Starter Code`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800, 400))

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()

        pygame.display.update()
    case_sensitive: true
    guided: false
  - question_id: 1
    question_type: qna
    question_text: |
      `What is the function to set the pygame window title`
    answer: pygame.display.set_caption('Runner')
    case_sensitive: false
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Add the window title to the pygame code`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800, 400))
      <<pygame.display.set_caption('Runner')>>

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()

        pygame.display.update()
    case_sensitive: true
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Modify code to set the frame rate`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800, 400))
      pygame.display.set_caption('Runner')
      <<clock = pygame.time.Clock()>>

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()

        pygame.display.update()
        <<clock.tick(60)>>
    case_sensitive: true
  - question_id: 4
    question_type: list_completion
    question_text: |
      `What are the 2 kinds of surfaces in pygame`
    answer:
      - "Display surfaces"
      - "regular surface"
    case_sensitive: true
    order_sensitive: false
  - question_id: 2
    question_type: fib
    question_text: "We can have <<only one>> display surfaces but <<multiple>> regular surfaces. Regular surface is visible only if <<its attached to the display surface>>."
    case_sensitive: true
  - question_id: 2
    question_type: fib
    question_text: "In order to create a colored surface you use the <<pygame.Surface((width,height))>> command. In order to set the color we use <<test_surface.fill('Red')>> command and to show the surface to the display we use <<screen.blit((test_surface),(0,0))>> command."
    case_sensitive: true
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Display a solid color surface`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800, 400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()

      <<test_surface = pygame.Surface((100,200))>>
      <<test_surface.fill('Red')>>

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()

        <<screen.blit(test_surface,(0,0))>>

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 1
    question_type: qna
    question_text: |
      `What is the function we use to get images as surface`
    answer: pygame.image.load('graphics/Sky.png')
    case_sensitive: false
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Modify this code to import an image`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800, 400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()

      <<sky_surface = pygame.image.load('graphics/Sky.png')>>

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()

        <<screen.blit(sky_surface,(0,0))>>

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 4
    question_type: list_completion
    question_text: |
      `List the three steps to display text in pygame.`
    answer:
      - "Make a font"
      - "Make text surface"
      - "Blit the text"
    case_sensitive: true
    order_sensitive: false
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Add text in the following code`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      test_font = <<pygame.font.Font(None, 50)>>

      sky_surface = pygame.image.load('graphics/Sky.png')
      ground_surface = pygame.image.load('graphics/ground.png')
      <<text_surface = test_font.render('My game', False, 'Green')>>

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        <<screen.blit(text_surface,(300,50))>>

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 1
    question_type: qna
    question_text: |
      `What is anti aliasing`
    answer: When computers smooth lines
    case_sensitive: false
  - question_id: 2
    question_type: fib
    question_text: "the second parameter in text_font.render('My game', False, 'Green') is for <<anti aliasing>>."
    case_sensitive: true
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Modify the following code so that is uses a specific font`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      # test_font = pygame.font.Font(None, 50)
      test_font = <<pygame.font.Font('font/Pixeltype.ttf', 50)>>

      sky_surface = pygame.image.load('graphics/Sky.png')
      ground_surface = pygame.image.load('graphics/ground.png')
      text_surface = test_font.render('My game', False, 'Green')

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        screen.blit(text_surface,(300,50))

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      Modify the following code such that:
      1. There is a snail image in the game. 
      2. The snail image moves left 4 pixels every frame
      3. If the snail x position is -100 it should go back to 800
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      test_font = pygame.font.Font('font/Pixeltype.ttf', 50)

      sky_surface = pygame.image.load('graphics/Sky.png')
      ground_surface = pygame.image.load('graphics/ground.png')
      text_surface = test_font.render('My game', False, 'Black')

      <<snail_surface = pygame.image.load('graphics/snail/snail1.png')>>
      snail_x_pos = 600

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        screen.blit(text_surface,(300,50))
        <<snail_x_pos -= 4>>
        <<if snail_x_pos < -100: snail_x_pos = 800>>
        <<screen.blit(snail_surface,(snail_x_pos,250))>> # height 250

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 4
    question_type: list_completion
    question_text: |
      `What are the two functions used to convert images to a more pygame frendly type`
    answer:
      - "convert"
      - "convert_alpha"
    case_sensitive: true
    order_sensitive: false
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Modify the following code to make the images more pygame frendly`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      test_font = pygame.font.Font('font/Pixeltype.ttf', 50)

      sky_surface = pygame.image.load('graphics/Sky.png')<<.convert()>>
      ground_surface = pygame.image.load('graphics/ground.png')<<.convert()>>
      text_surface = test_font.render('My game', False, 'Black')

      snail_surface = pygame.image.load('graphics/snail/snail1.png')<<.convert_alpha()>>
      snail_x_pos = 600

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        screen.blit(text_surface,(300,50))
        snail_x_pos -= 4
        if snail_x_pos < -100: snail_x_pos = 800
        screen.blit(snail_surface,(snail_x_pos,250))

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 4
    question_type: list_completion
    question_text: |
      `What are the two main functions of lists.`
    answer:
      - "Precise positioning of surfaces"
      - "Basic Collisions"
    case_sensitive: true
    order_sensitive: false
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      In the following code.
      1. Use rectangle to place the player character using the bottom right point. 
      2. Use rectangle to place the snail using mid bottom.
      3. Use the right property to check if the snail is going beyond the screen
      4. Use left the place the snail in the correct position. 
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      test_font = pygame.font.Font('font/Pixeltype.ttf', 50)

      sky_surface = pygame.image.load('graphics/Sky.png').convert()
      ground_surface = pygame.image.load('graphics/ground.png').convert()
      text_surface = test_font.render('My game', False, 'Black')

      # snail_surface = pygame.image.load('graphics/snail/snail1.png').convert_alpha()
      # snail_x_pos = 600

      snail_surf = <<pygame.image.load("graphics/snail/snail1.png").convert_alpha()>>
      <<snail_rect = snail_surf.get_rect(bottmright = (600,300))>>

      player_surf = <<pygame.image.load('graphics/player/player_walk_1.png').convert_alpha()>>
      <<player_rect = player_surf.get_rect(midbottom = (80,300))>>

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        screen.blit(text_surface,(300,50))

        snail_x_pos -= 4
        <<if snail_rect.right <= 0: snail_rect.left = 800>>
        <<screen.blit(snail_surf,snail_rect)>>
        <<screen.blit(player_surf,player_rect)>>
        
        # if snail_x_pos < -100: snail_x_pos = 800
        # screen.blit(snail_surface,(snail_x_pos,250))

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 1
    question_type: qna
    question_text: |
      `What is the function to check if a collision is happening`
    answer: rect1.colliderect(rect2)
    case_sensitive: false
  - question_id: 2
    question_type: fib
    question_text: "rect1.colliderect(rect2) returns <<0>> if there is no collision otherwise it returns a <<1>>."
    case_sensitive: true
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Modify the following code to check if a collision between the snail and the player is happening.`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      test_font = pygame.font.Font('font/Pixeltype.ttf', 50)

      sky_surface = pygame.image.load('graphics/Sky.png').convert()
      ground_surface = pygame.image.load('graphics/ground.png').convert()
      text_surface = test_font.render('My game', False, 'Black')

      # snail_surface = pygame.image.load('graphics/snail/snail1.png').convert_alpha()
      # snail_x_pos = 600

      snail_surf = pygame.image.load("graphics/snail/snail1.png").convert_alpha()
      snail_rect = snail_surf.get_rect(bottmright = (600,300))

      player_surf = pygame.image.load('graphics/player/player_walk_1.png').convert_alpha()
      player_rect = player_surf.get_rect(midbottom = (80,300))

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        screen.blit(text_surface,(300,50))

        snail_x_pos -= 4
        if snail_rect.right <= 0: snail_rect.left = 800
        screen.blit(snail_surf,snail_rect)
        screen.blit(player_surf,player_rect)
 
        <<if player_rect.colliderect(snail_rect):>>
          <<print('collision')>>

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 1
    question_type: qna
    question_text: |
      `How do we check the collision of a rectangle and a point?`
    answer: rect.collidepoint((x,y))
    case_sensitive: false
  - question_id: 4
    question_type: list_completion
    question_text: |
      `What are the two methods to get the mouse position?`
    answer:
      - "pygame.mouse"
      - "event loop"
    case_sensitive: true
    order_sensitive: false
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Modify the code to check if the player collides with the mouse.`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      test_font = pygame.font.Font('font/Pixeltype.ttf', 50)

      sky_surface = pygame.image.load('graphics/Sky.png').convert()
      ground_surface = pygame.image.load('graphics/ground.png').convert()
      text_surface = test_font.render('My game', False, 'Black')

      snail_surf = pygame.image.load("graphics/snail/snail1.png").convert_alpha()
      snail_rect = snail_surf.get_rect(bottmright = (600,300))

      player_surf = pygame.image.load('graphics/player/player_walk_1.png').convert_alpha()
      player_rect = player_surf.get_rect(midbottom = (80,300))

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        screen.blit(text_surface,(300,50))

        snail_x_pos -= 4
        if snail_rect.right <= 0: snail_rect.left = 800
        screen.blit(snail_surf,snail_rect)
        screen.blit(player_surf,player_rect)
 
        # if player_rect.colliderect(snail_rect):
        #   print('collision')

        <<mouse_pos = pygame.mouse.get_pos()>>
        <<if player_rect.collidepoint(mouse_pos):>>
          <<print('collision')>>

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 1
    question_type: qna
    question_text: |
      `What is the mouse function the check if the mouse button was pressed`
    answer: pygame.mouse.get_pressed()
    case_sensitive: false
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Modify the following code to check which mouse button is pressed when the mouse hovers over the player`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      test_font = pygame.font.Font('font/Pixeltype.ttf', 50)

      sky_surface = pygame.image.load('graphics/Sky.png').convert()
      ground_surface = pygame.image.load('graphics/ground.png').convert()
      text_surface = test_font.render('My game', False, 'Black')

      snail_surf = pygame.image.load("graphics/snail/snail1.png").convert_alpha()
      snail_rect = snail_surf.get_rect(bottmright = (600,300))

      player_surf = pygame.image.load('graphics/player/player_walk_1.png').convert_alpha()
      player_rect = player_surf.get_rect(midbottom = (80,300))

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        screen.blit(text_surface,(300,50))

        snail_x_pos -= 4
        if snail_rect.right <= 0: snail_rect.left = 800
        screen.blit(snail_surf,snail_rect)
        screen.blit(player_surf,player_rect)
 
        # if player_rect.colliderect(snail_rect):
        #   print('collision')

        <<mouse_pos = pygame.mouse.get_pos()>>
        <<if player_rect.collidepoint(mouse_pos):>>
          <<pygame.mouse.get_pressed()>>

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true
  - question_id: 7
    question_type: code_fib
    language: python
    question_text: |
      `Modify the following code such that using events we get the mouse position if the mouse moves`
    question_code: |
      import pygame
      from sys import exit

      pygame.init()
      screen = pygame.display.set_mode((800,400))
      pygame.display.set_caption('Runner')
      clock = pygame.time.Clock()
      test_font = pygame.font.Font('font/Pixeltype.ttf', 50)

      sky_surface = pygame.image.load('graphics/Sky.png').convert()
      ground_surface = pygame.image.load('graphics/ground.png').convert()
      text_surface = test_font.render('My game', False, 'Black')

      snail_surf = pygame.image.load("graphics/snail/snail1.png").convert_alpha()
      snail_rect = snail_surf.get_rect(bottmright = (600,300))

      player_surf = pygame.image.load('graphics/player/player_walk_1.png').convert_alpha()
      player_rect = player_surf.get_rect(midbottom = (80,300))

      while True:
        for event in pygame.event.get():
          if event.type == pygame.QUIT:
            pygame.quit()
            exit()
          if event.type == pygame.MOUSEMOTION:
            print(event.pos)
        
        screen.blit(sky_surface,(0,0))
        screen.blit(ground_surface,(0,300))
        screen.blit(text_surface,(300,50))

        snail_x_pos -= 4
        if snail_rect.right <= 0: snail_rect.left = 800
        screen.blit(snail_surf,snail_rect)
        screen.blit(player_surf,player_rect)
 

        pygame.display.update()
        clock.tick(60)
    case_sensitive: true

  